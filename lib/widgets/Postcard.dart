import 'package:flutter/cupertino.dart';
import 'package:easy_quick/JsonParse/posts.dart';

class Postcard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      // build a more fitting UI
    );
  }
}
